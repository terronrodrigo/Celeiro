# Resend - envio de emails (obrigatório para enviar)
# Crie em https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxx

# Domínio verificado no Resend para "from" (ex: noreply@seudominio.com)
RESEND_FROM_EMAIL=Celeiro São Paulo <noreply@seudominio.com>

# Planilha Google - use uma destas opções:
# 1) Planilha publicada na web: publique em Arquivo > Compartilhar > Publicar na Web (aba "Links")
# 2) URL direta do CSV (após publicar):
GOOGLE_SHEETS_CSV_URL=https://docs.google.com/spreadsheets/d/1uTgaI8Ct_rPr1KwyDOPCH5SLqdzv0Bwxog0B9k-PbPo/export?format=csv&gid=1582636562

# CSV local de voluntários (se não usar Google Sheets)
VOLUNTARIOS_CSV_PATH=

# CSV local de check-in (obrigatório para o módulo de check-in)
CHECKIN_CSV_PATH=

# Caminho do banco SQLite (opcional)
DB_PATH=

# MongoDB - para gerenciar usuários
# Local: mongodb://localhost:27017/celeiro-dashboard
# MongoDB Atlas (cloud): mongodb+srv://usuario:senha@cluster.mongodb.net/celeiro-dashboard
MONGODB_URI=mongodb://localhost:27017/celeiro-dashboard

# Credenciais admin (obrigatório para acessar)
ADMIN_USER=admin
ADMIN_PASS=admin123

# Credenciais Google OAuth (opcional, para login com Google)
GOOGLE_CLIENT_ID=seu-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=seu-google-client-secret

# Tempo de sessão (horas)
AUTH_TOKEN_TTL_HOURS=24

# Porta do servidor (em cloud a plataforma define PORT automaticamente)
PORT=3001

# Produção: use MONGODB_URI do MongoDB Atlas e senhas fortes para ADMIN_*
